# Dotfile backups the installer makes
*.backup.*

# Setup log
launchpad-setup.log

# macOS cruft
.DS_Store

# Local env files (never commit secrets)
.env
.env.local

# here.now local state and credentials
.herenow/

# Local browser-test artifacts (Playwright MCP)
.playwright-mcp/
/*.png

